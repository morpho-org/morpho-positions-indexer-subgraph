{
  "name": "morpho-positions-indexer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ morpho-positions-indexer",
    "create-local": "graph create --node http://localhost:8020/ morpho-positions-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ morpho-positions-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 morpho-positions-indexer",
    "test": "graph test",
    "lint": "yarn biome format --write ./src"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "matchstick-as": "0.5.0"
  }
}
